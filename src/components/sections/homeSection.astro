---
import Icon from '../icon.astro';

export interface Props {
	title: string;
}

const { title } = Astro.props;
---

<section class="homeSection">
	<div class="container">
		<div class="homeSection__content">
			<div class="homeSection__title">
				{title ? <h1>{title}</h1> : 'Astro Template Starter'}
			</div>
		</div>
	</div>
</section>